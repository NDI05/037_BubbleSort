#include <iostream>
using namespace std;

int arr[20];
int n;

void inputArrayData(){

};

void bubbleSort(){
    int pass = 1
    do{
        (for int j = 0; j <= n-1-pass; j++){
            if(arr[j] > arr[j+1]){
                int temp
                tempt = arr[j];
                arr[j] = arr[j+1];
                arr[j+1] = temp;
            }
        }
    }while(pass <= n-1);
};

void display(){

};

int main(){

};